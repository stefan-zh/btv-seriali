<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="ExoMediaButton.FS_Enter">
        <item name="android:src">@drawable/exo_controls_fullscreen_enter</item>
        <item name="android:contentDescription">@string/exo_controls_fullscreen_description</item>
    </style>

    <!-- Theme for the Expanded Controller -->
    <style name="Theme.CastVideos" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- https://developers.google.com/android/reference/com/google/android/gms/cast/framework/media/widget/ExpandedControllerActivity#set-toolbar-theme -->
        <item name="castExpandedControllerToolbarStyle">@style/ThemeOverlay.AppCompat.Dark.ActionBar</item>
    </style>

</resources>
